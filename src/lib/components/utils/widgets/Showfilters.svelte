<!-- <script>
    import { Ruler } from '$lib/components';
    import { createEventDispatcher } from 'svelte';

    const dispatch = createEventDispatcher();


    let toggleFilters =()=> {
        dispatch('toggle', {
			
		});
    }
</script>

<Ruler>
    <div class="bg-[#393939] px-2 py-2 rounded-lg w-32 md:hidden text-white">
        <button on:click={toggleFilters}>
            Show Filters
        </button>
    </div>
</Ruler> -->